export interface AddCustomerDialogProps {
    isOpen: boolean
    onClose: () => void
}

const AddCustomerDialog = ({ }: AddCustomerDialogProps) => {
    return (
        <div>

        </div>
    )
}